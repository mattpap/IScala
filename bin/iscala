#! /bin/bash

JAR="IScala.jar"

if [ -e "$(dirname $0)/$JAR" ]; then
    java -jar "$(dirname $0)/$JAR" "$@"
elif [ -e "$(pwd)/$JAR" ]; then
    java -jar "$(pwd)/$JAR" "$@"
else
    echo "$JAR doesn't exist. Run './sbt assembly' to generate it." >&2
    exit 1
fi
